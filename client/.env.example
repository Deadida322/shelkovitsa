# Базовый URL API для Nuxt (без суффикса /api). Подхватывается при npm run build.
# Локально:
BASE_URL=http://localhost:8000

# Продакшен (подставьте свой домен с https):
# BASE_URL=https://test.shelkovitsa.ru
